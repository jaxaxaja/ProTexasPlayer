#include<strategy/FlopStrategy.h>

std::unique_ptr<Move> FlopStrategy::callRaiseOrFold(float bb, const Board &board, const Hand &hand)
{
    return nullptr;
}

std::unique_ptr<Move> FlopStrategy::checkOrBet(const Board &board, const Hand &hand)
{
    return nullptr;
}
