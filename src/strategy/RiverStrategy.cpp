#include<strategy/RiverStrategy.h>

std::unique_ptr<Move> RiverStrategy::callRaiseOrFold(float bb, const Board &board, const Hand &hand)
{
    return nullptr;
}

std::unique_ptr<Move> RiverStrategy::checkOrBet(const Board &board, const Hand &hand)
{
    return nullptr;
}
